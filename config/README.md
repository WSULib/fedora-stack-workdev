Configuration files you need to port around can be placed here. For example, config is here, and _config would be here, too. Other files for inclusion might be a Solr schema or other non-sensitive data. 
